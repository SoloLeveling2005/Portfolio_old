import React, { useEffect, useState } from 'react';
import '../App.css';
import { Link } from 'react-router-dom';

function Notification(props:{array_notifications:[]}) {
    const array_notifications = props.array_notifications
    return (
        <div>

        </div>
    );
}

export default Notification;
